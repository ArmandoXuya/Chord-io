import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ChordsApp_',
  appName: 'Chordify-Ion',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
